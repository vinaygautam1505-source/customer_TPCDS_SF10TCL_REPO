
name: 'PROJECT_customer_TPCDS_SF100TCL'
version: '1.0.0'
config-version: 2

profile: 'PROJECT_customer_TPCDS_SF100TCL'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  my_dbt_project:
    staging:
      +materialized: view
    marts:
      +materialized: table
